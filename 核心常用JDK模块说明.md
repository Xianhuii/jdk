# JDK最核心、最常用模块说明

本文件简要介绍JDK源码（/src目录）下最核心、最常用的模块，便于理解JDK整体架构和常用API分布。

---

## 1. java.base
- **地位**：JDK的根模块，所有其它模块都依赖它。
- **主要内容**：
  - Java语言基础类库：`java.lang`、`java.util`、`java.io`、`java.net`、`java.nio`、`java.security`等。
- **作用**：
  - 字符串、集合、I/O、并发、反射、基础安全等，Java程序的基石。
- **典型应用场景**：所有Java应用，无需在module-info.java中声明，自动依赖。

---

## 2. java.logging
- **主要内容**：Java标准日志框架（`java.util.logging`）。
- **作用**：日志记录，很多中间件和应用依赖。
- **典型应用场景**：服务端、桌面、工具类应用的日志输出。

---

## 3. java.xml
- **主要内容**：XML解析与处理（DOM、SAX、XPath、XSLT等）。
- **作用**：配置、数据交换等场景常用。
- **典型应用场景**：配置文件解析、Web服务、数据交换。

---

## 4. java.sql
- **主要内容**：JDBC数据库访问API。
- **作用**：数据库操作，企业开发必备。
- **典型应用场景**：企业级应用、数据持久化。

---

## 5. java.desktop
- **主要内容**：图形界面（AWT/Swing）、剪贴板、打印等。
- **作用**：桌面应用开发。
- **典型应用场景**：桌面软件、可视化工具。

---

## 6. java.compiler
- **主要内容**：Java编译器API、注解处理、语言模型。
- **作用**：IDE、构建工具、动态编译、注解处理等。
- **典型应用场景**：IDE、代码生成、注解处理器开发。

---

## 7. jdk.hotspot.agent
- **主要内容**：HotSpot Serviceability Agent（SA），调试、诊断JVM。
- **作用**：JVM诊断、调试、分析工具（如jhsdb）。
- **典型应用场景**：线上/离线JVM分析、故障排查。

---

## 8. jdk.compiler
- **主要内容**：javac编译器实现。
- **作用**：Java源码编译为字节码。
- **典型应用场景**：命令行编译、IDE集成编译。

---

## 9. jdk.management
- **主要内容**：JVM管理与监控接口（JMX、MBeans）。
- **作用**：JVM监控、远程管理。
- **典型应用场景**：运维监控、远程诊断。

---

## 10. jdk.attach
- **主要内容**：JVM进程动态attach支持。
- **作用**：诊断工具、监控工具动态连接运行中的JVM。
- **典型应用场景**：jcmd、jmap、jstack等工具。

---

## 总结
- **java.base** 是唯一不可或缺的基础模块，所有Java程序都依赖。
- 其它模块根据实际应用场景选用，是Java开发的常用基础。

如需详细了解某个模块，请查阅对应源码或官方文档。